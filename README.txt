to run fastapi backend: 
1. cd into backend
2. run uvicorn main:app --host 0.0.0.0 --port 8000 --reload

to run frontend: npm run dev
1. cd into frontend
2. run npm run dev

backend dependencies:
pip install fastapi uvicorn opencv-python numpy starlette
